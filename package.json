{
  "name": "poc-chatgpt",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-models": "echo 'Updating models.json / models.txt' && curl --silent https://api.openai.com/v1/models -H \"Authorization: Bearer $OPENAI_API_KEY\" > models.json && jq -r '.data[].id' models.json > models.txt && echo 'Done!'",
    "extract-models-by-owned-by": "echo 'Extracting models from models.json and grouping them by owned_by' && jq -r '.data | sort_by(-.created) | map({ id: .id, owned_by: .owned_by, created: .created, created_iso8601: (.created | todate) }) | group_by(.owned_by) | map({ (.[0].owned_by): . }) | add' models.json > models_by_owned_by.json && echo 'Extracted models saved to models_by_owned_by.json'",
    "update-engines": "echo 'Updating engines.json / engines.txt' && curl --silent https://api.openai.com/v1/engines -H \"Authorization: Bearer $OPENAI_API_KEY\" > engines.json && jq -r '.data[].id' engines.json > engines.txt && echo 'Done!'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "openai": "^3.1.0"
  }
}
